"""
Setup script for your project.

Run with pip install -e . to setup the project directory as a package in
your environment.
"""

from distutils.core import setup
# from setuptools import find_packages

setup(
    name="python_template",
    version="0.0.1",
    description="Simple TDD project"
)
